{
  "name": "samples",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "./*"
  ],
  "devDependencies": {
    "turbo": "^1.13.3"
  },
  "scripts": {
    "generate-api": "turbo run generate-api --filter=!nx-fastify-react"
  }
}
