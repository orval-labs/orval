/*
 * Generated by orval v3.2.1 🍺
 * Do not edit manually.
 * Swagger Petstore
 * OpenAPI spec version: 1.0.0
 */
import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { CreatePetsBody, ListPetsParams, Pet, Pets } from '../../model';
import { IPetsService } from './pets.definition';

@Injectable()
export class PetsService implements IPetsService {
  constructor(private http: HttpClient) {}
  listPets(params?: ListPetsParams, version: number = 1): Observable<Pets> {
    type Mutator = (
      url: string,
      config?: object,
    ) => [string, object | undefined];

    const mutator: Mutator = (url, config) => [
      url,
      { ...config, responseType: 'json' },
    ];

    return this.http.get<Pets>(
      ...mutator(`/v${version}/pets`, {
        params,
      }),
    );
  }
  createPets(
    createPetsBody: CreatePetsBody,
    version: number = 1,
  ): Observable<unknown> {
    return this.http.post<unknown>(`/v${version}/pets`, createPetsBody);
  }
  showPetById(petId: string, version: number = 1): Observable<Pet> {
    return this.http.get<Pet>(`/v${version}/pets/${petId}`);
  }
}
