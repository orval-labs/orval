/**
 * Generated by orval v8.13.0 🍺
 * Do not edit manually.
 * Petstore with dynamic references
 * Demonstrates OpenAPI 3.1 `$dynamicRef` / `$dynamicAnchor` for polymorphic schema dispatch.
 * The `Pet` base schema defines a `$dynamicAnchor` that is overridden by `Cat` and `Dog`.  Each schema has a `playmates` property using `$dynamicRef: '#Pet'` — which resolves to `Cat[]` inside Cat, and `Dog[]` inside Dog.
 * `Lizard` does NOT override the anchor, so its `playmates` falls back to `Pet[]` via the `$dynamicAnchor` fallback per JSON Schema Draft 2020-12.
 *
 * OpenAPI spec version: 1.0.0
 */
import axios from 'axios';
import type { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios';

export interface Pet {
  petType: string;
  name: string;
  playmates?: Pet[];
}

export type CatPetType = (typeof CatPetType)[keyof typeof CatPetType];

export const CatPetType = {
  cat: 'cat',
} as const;

export interface Cat {
  petType: CatPetType;
  name: string;
  meow: boolean;
  playmates?: Cat[];
}

export type DogPetType = (typeof DogPetType)[keyof typeof DogPetType];

export const DogPetType = {
  dog: 'dog',
} as const;

export interface Dog {
  petType: DogPetType;
  name: string;
  bark: boolean;
  playmates?: Dog[];
}

export type LizardPetType = (typeof LizardPetType)[keyof typeof LizardPetType];

export const LizardPetType = {
  lizard: 'lizard',
} as const;

export interface Lizard {
  petType: LizardPetType;
  name: string;
  lovesRocks?: boolean;
  playmates?: Pet[];
}

export const getPetstoreWithDynamicReferences = (
  axiosInstance: AxiosInstance = axios,
) => {
  /**
   * @summary List all pets
   */
  const listPets = (
    options?: AxiosRequestConfig,
  ): Promise<AxiosResponse<Pet[]>> => {
    return axiosInstance.get(`/pets`, options);
  };

  /**
   * @summary Get a pet by ID
   */
  const showPetById = (
    petId: number,
    options?: AxiosRequestConfig,
  ): Promise<AxiosResponse<Pet>> => {
    return axiosInstance.get(`/pets/${petId}`, options);
  };

  return { listPets, showPetById };
};
export type ListPetsResult = AxiosResponse<Pet[]>;
export type ShowPetByIdResult = AxiosResponse<Pet>;
