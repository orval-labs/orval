/**
 * Generated by orval v8.7.0 🍺
 * Do not edit manually.
 * Swagger Petstore - OpenAPI 3.0
 * This is a sample Pet Store Server based on the OpenAPI 3.0 specification.  You can find out more about
Swagger at [https://swagger.io](https://swagger.io). In the third iteration of the pet store, we've switched to the design first approach!
You can now help us improve the API whether it's by making changes to the definition itself or to the code.
That way, with time, we can improve the API in general, and expose some of the new features in OAS3.

Some useful links:
- [The Pet Store repository](https://github.com/swagger-api/swagger-petstore)
- [The source API definition for the Pet Store](https://github.com/swagger-api/swagger-petstore/blob/master/src/main/resources/openapi.yaml)
 * OpenAPI spec version: 1.0.27-SNAPSHOT
 */
export const getFindPetsByStatusUrl = (params) => {
  const normalizedParams = new URLSearchParams();
  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });
  const stringifiedParams = normalizedParams.toString();
  return stringifiedParams.length > 0
    ? `https://petstore3.swagger.io/api/v3/pet/findByStatus?${stringifiedParams}`
    : `https://petstore3.swagger.io/api/v3/pet/findByStatus`;
};
export const findPetsByStatus = async (params, options) => {
  const res = await fetch(getFindPetsByStatusUrl(params), {
    ...options,
    method: 'GET',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
export const getFindPetsByTagsUrl = (params) => {
  const normalizedParams = new URLSearchParams();
  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ['tags'];
    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? 'null' : v.toString());
      });
      return;
    }
  });
  const stringifiedParams = normalizedParams.toString();
  return stringifiedParams.length > 0
    ? `https://petstore3.swagger.io/api/v3/pet/findByTags?${stringifiedParams}`
    : `https://petstore3.swagger.io/api/v3/pet/findByTags`;
};
export const findPetsByTags = async (params, options) => {
  const res = await fetch(getFindPetsByTagsUrl(params), {
    ...options,
    method: 'GET',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
export const getGetPetByIdUrl = (petId) => {
  return `https://petstore3.swagger.io/api/v3/pet/${petId}`;
};
export const getPetById = async (petId, options) => {
  const res = await fetch(getGetPetByIdUrl(petId), {
    ...options,
    method: 'GET',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
export const getUpdatePetWithFormUrl = (petId, params) => {
  const normalizedParams = new URLSearchParams();
  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });
  const stringifiedParams = normalizedParams.toString();
  return stringifiedParams.length > 0
    ? `https://petstore3.swagger.io/api/v3/pet/${petId}?${stringifiedParams}`
    : `https://petstore3.swagger.io/api/v3/pet/${petId}`;
};
export const updatePetWithForm = async (petId, params, options) => {
  const res = await fetch(getUpdatePetWithFormUrl(petId, params), {
    ...options,
    method: 'POST',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
export const getDeletePetUrl = (petId) => {
  return `https://petstore3.swagger.io/api/v3/pet/${petId}`;
};
export const deletePet = async (petId, options) => {
  const res = await fetch(getDeletePetUrl(petId), {
    ...options,
    method: 'DELETE',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
export const getGetInventoryUrl = () => {
  return `https://petstore3.swagger.io/api/v3/store/inventory`;
};
export const getInventory = async (options) => {
  const res = await fetch(getGetInventoryUrl(), {
    ...options,
    method: 'GET',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
export const getGetOrderByIdUrl = (orderId) => {
  return `https://petstore3.swagger.io/api/v3/store/order/${orderId}`;
};
export const getOrderById = async (orderId, options) => {
  const res = await fetch(getGetOrderByIdUrl(orderId), {
    ...options,
    method: 'GET',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
export const getDeleteOrderUrl = (orderId) => {
  return `https://petstore3.swagger.io/api/v3/store/order/${orderId}`;
};
export const deleteOrder = async (orderId, options) => {
  const res = await fetch(getDeleteOrderUrl(orderId), {
    ...options,
    method: 'DELETE',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
export const getLoginUserUrl = (params) => {
  const normalizedParams = new URLSearchParams();
  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });
  const stringifiedParams = normalizedParams.toString();
  return stringifiedParams.length > 0
    ? `https://petstore3.swagger.io/api/v3/user/login?${stringifiedParams}`
    : `https://petstore3.swagger.io/api/v3/user/login`;
};
export const loginUser = async (params, options) => {
  const res = await fetch(getLoginUserUrl(params), {
    ...options,
    method: 'GET',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
export const getLogoutUserUrl = () => {
  return `https://petstore3.swagger.io/api/v3/user/logout`;
};
export const logoutUser = async (options) => {
  const res = await fetch(getLogoutUserUrl(), {
    ...options,
    method: 'GET',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
export const getGetUserByNameUrl = (username) => {
  return `https://petstore3.swagger.io/api/v3/user/${username}`;
};
export const getUserByName = async (username, options) => {
  const res = await fetch(getGetUserByNameUrl(username), {
    ...options,
    method: 'GET',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
export const getDeleteUserUrl = (username) => {
  return `https://petstore3.swagger.io/api/v3/user/${username}`;
};
export const deleteUser = async (username, options) => {
  const res = await fetch(getDeleteUserUrl(username), {
    ...options,
    method: 'DELETE',
  });
  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  const data = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  };
};
