/**
 * Generated by orval v8.18.0 🍺
 * Do not edit manually.
 * Faker schemas string enum ref
 * Reproduces the case where a parent schema references a string-typed enum
 * component via $ref, so the generated faker mock calls Object.values(EnumName)
 * at runtime. Without the fix, the consolidated index.faker.ts imports the
 * enum as `import type { ... }`, causing TS1361.
 *
 * OpenAPI spec version: 1.0.0
 */
import { faker } from '@faker-js/faker';

import { DisplayColor } from '../index.schemas';
import type { DisplayValueDto } from '../index.schemas';

export const getGetDisplayResponseMock = (
  overrideResponse: Partial<Extract<DisplayValueDto, object>> = {},
): DisplayValueDto => ({
  color: faker.helpers.arrayElement([
    faker.helpers.arrayElement(Object.values(DisplayColor)),
    undefined,
  ]),
  value: faker.string.alpha({ length: { min: 10, max: 20 } }),
  boldPart: faker.helpers.arrayElement([
    faker.string.alpha({ length: { min: 10, max: 20 } }),
    undefined,
  ]),
  ...overrideResponse,
});
