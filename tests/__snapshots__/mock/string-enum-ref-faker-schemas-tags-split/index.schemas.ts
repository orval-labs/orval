/**
 * Generated by orval v8.12.3 🍺
 * Do not edit manually.
 * Faker schemas string enum ref
 * Reproduces the case where a parent schema references a string-typed enum
 * component via $ref, so the generated faker mock calls Object.values(EnumName)
 * at runtime. Without the fix, the consolidated index.faker.ts imports the
 * enum as `import type { ... }`, causing TS1361.
 *
 * OpenAPI spec version: 1.0.0
 */
/**
 * A text color that can be used for display purposes.
 */
export type DisplayColor = (typeof DisplayColor)[keyof typeof DisplayColor];

export const DisplayColor = {
  TEXT_01: 'TEXT_01',
  TEXT_04: 'TEXT_04',
  TEXT_CURRENCY_GAIN: 'TEXT_CURRENCY_GAIN',
} as const;

export interface DisplayValueDto {
  color?: DisplayColor;
  value: string;
  boldPart?: string;
}
