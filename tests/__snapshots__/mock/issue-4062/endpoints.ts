/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Issue 4062 - binary faker mocks must match the generated Blob type
 * OpenAPI spec version: 1.0.0
 */
import type { Upload } from './model';

export type createUploadResponse200 = {
  data: Upload;
  status: 200;
};

export type createUploadResponseSuccess = createUploadResponse200 & {
  headers: Headers;
};
export type createUploadResponse = createUploadResponseSuccess;

export const getCreateUploadUrl = () => {
  return `/uploads`;
};

export const createUpload = async (
  upload?: Upload,
  options?: RequestInit,
): Promise<createUploadResponse> => {
  const formData = new FormData();
  if (upload?.name !== undefined) {
    formData.append(`name`, upload.name);
  }
  if (upload?.file !== undefined) {
    formData.append(`file`, upload.file);
  }
  if (upload?.attachment !== undefined) {
    formData.append(`attachment`, upload.attachment);
  }

  const res = await fetch(getCreateUploadUrl(), {
    ...options,
    method: 'POST',
    body: formData,
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: createUploadResponse['data'] = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  } as createUploadResponse;
};

export type downloadUploadResponse200 = {
  data: Blob;
  status: 200;
};

export type downloadUploadResponseSuccess = downloadUploadResponse200 & {
  headers: Headers;
};
export type downloadUploadResponse = downloadUploadResponseSuccess;

export const getDownloadUploadUrl = (id: string) => {
  return `/uploads/${id}/content`;
};

export const downloadUpload = async (
  id: string,
  options?: RequestInit,
): Promise<downloadUploadResponse> => {
  const res = await fetch(getDownloadUploadUrl(id), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.blob();
  const data: downloadUploadResponse['data'] =
    body as downloadUploadResponse['data'];
  return {
    data,
    status: res.status,
    headers: res.headers,
  } as downloadUploadResponse;
};
