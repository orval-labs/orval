/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AllOf narrowed enum
 * Regression: when an allOf member narrows an inherited enum property to a
 * single literal without re-listing it in its own `required`, the mock
 * generator used to treat the property as optional and emit
 * `faker.helpers.arrayElement([..., undefined])`, which is not assignable to
 * the composed type (the property stays required through the base schema).
 * OpenAPI spec version: 1.0.0
 */
import { faker } from '@faker-js/faker';

import type {
  AliasedValidationError,
  BaseError,
  BaseErrorAlias,
  Item,
  ItemDetail,
  SiblingValidationError,
  ValidationError,
} from '.';

export const getBaseErrorMock = (
  overrideResponse: Partial<BaseError> = {},
): BaseError => ({
  errorType: faker.helpers.arrayElement([
    'GENERIC_ERROR',
    'VALIDATION_ERROR',
  ] as const),
  message: faker.string.alpha({ length: { min: 10, max: 20 } }),
  ...overrideResponse,
});

export const getValidationErrorMock = (): ValidationError => ({
  ...{ ...getBaseErrorMock() },
  ...{
    errorType: faker.helpers.arrayElement(['VALIDATION_ERROR'] as const),
    violations: Array.from(
      { length: faker.number.int({ min: 1, max: 10 }) },
      (_, i) => i + 1,
    ).map(() => ({
      field: faker.helpers.arrayElement([
        faker.string.alpha({ length: { min: 10, max: 20 } }),
        undefined,
      ]),
      message: faker.helpers.arrayElement([
        faker.string.alpha({ length: { min: 10, max: 20 } }),
        undefined,
      ]),
    })),
  },
});

export const getBaseErrorAliasMock = (): BaseErrorAlias => ({
  ...getBaseErrorMock(),
});

export const getAliasedValidationErrorMock = (): AliasedValidationError => ({
  ...{ ...getBaseErrorAliasMock() },
  ...{ errorType: faker.helpers.arrayElement(['VALIDATION_ERROR'] as const) },
});

export const getSiblingValidationErrorMock = (): SiblingValidationError => ({
  ...{ ...{ ...getBaseErrorMock() } },
  errorType: faker.helpers.arrayElement(['VALIDATION_ERROR'] as const),
});

export const getItemMock = (overrideResponse: Partial<Item> = {}): Item => ({
  id: faker.helpers.arrayElement([
    faker.string.alpha({ length: { min: 10, max: 20 } }),
    undefined,
  ]),
  name: faker.helpers.arrayElement([
    faker.string.alpha({ length: { min: 10, max: 20 } }),
    undefined,
  ]),
  ...overrideResponse,
});

export const getItemDetailMock = (): ItemDetail => ({
  ...{
    id: faker.string.alpha({ length: { min: 10, max: 20 } }),
    name: faker.string.alpha({ length: { min: 10, max: 20 } }),
  },
});
