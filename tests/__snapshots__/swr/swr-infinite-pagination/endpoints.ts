/**
 * Generated by orval v8.4.2 🍺
 * Do not edit manually.
 * SWR Infinite Pagination Test
 * Test various response types for useSWRInfinite pagination termination
 * OpenAPI spec version: 1.0.0
 */
import useSwr from 'swr';
import type { Key, SWRConfiguration } from 'swr';

import useSWRInfinite from 'swr/infinite';
import type {
  SWRInfiniteConfiguration,
  SWRInfiniteKeyLoader,
} from 'swr/infinite';

import type {
  ListPetsArrayParams,
  ListPetsWrappedParams,
  Pet,
  PetsWrappedResponse,
} from './model';

/**
 * @summary List all pets as direct array
 */
export type listPetsArrayResponse200 = {
  data: Pet[];
  status: 200;
};

export type listPetsArrayResponseSuccess = listPetsArrayResponse200 & {
  headers: Headers;
};
export type listPetsArrayResponse = listPetsArrayResponseSuccess;

export const getListPetsArrayUrl = (params?: ListPetsArrayParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/pets-array?${stringifiedParams}`
    : `/pets-array`;
};

export const listPetsArray = async (
  params?: ListPetsArrayParams,
  options?: RequestInit,
): Promise<listPetsArrayResponse> => {
  const res = await fetch(getListPetsArrayUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: listPetsArrayResponse['data'] = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  } as listPetsArrayResponse;
};

export const getListPetsArrayKey = (params?: ListPetsArrayParams) =>
  [`/pets-array`, ...(params ? [params] : [])] as const;
export const getListPetsArrayInfiniteKeyLoader = (
  params?: ListPetsArrayParams,
) => {
  return (
    page: number,
    previousPageData?: Awaited<ReturnType<typeof listPetsArray>>,
  ) => {
    if (previousPageData) {
      const responseData = previousPageData.data;
      if (!responseData) return null;

      // Direct array response (e.g., API returns Pet[]) - stop when empty
      if (Array.isArray(responseData) && responseData.length === 0) return null;

      // Wrapped response with data array (e.g., { data: Pet[], ... }) - stop when nested array is empty
      if (typeof responseData === 'object' && 'data' in responseData) {
        if (Array.isArray(responseData.data) && responseData.data.length === 0)
          return null;
      }

      // Single object response (non-paginated endpoint) - stop after first page
      if (
        !Array.isArray(responseData) &&
        !(typeof responseData === 'object' && 'data' in responseData)
      )
        return null;
    }

    return [
      `/pets-array`,
      ...(params ? [{ ...params, page }] : [{ page }]),
    ] as const;
  };
};

export type ListPetsArrayInfiniteQueryResult = NonNullable<
  Awaited<ReturnType<typeof listPetsArray>>
>;

/**
 * @summary List all pets as direct array
 */
export const useListPetsArrayInfinite = <TError = Promise<unknown>>(
  params?: ListPetsArrayParams,
  options?: {
    swr?: SWRInfiniteConfiguration<
      Awaited<ReturnType<typeof listPetsArray>>,
      TError
    > & { swrKeyLoader?: SWRInfiniteKeyLoader; enabled?: boolean };
    fetch?: RequestInit;
  },
) => {
  const { swr: swrOptions, fetch: fetchOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKeyLoader =
    swrOptions?.swrKeyLoader ??
    (isEnabled ? getListPetsArrayInfiniteKeyLoader(params) : () => null);
  const swrFn = ([_url, pageParams]: [
    string,
    ListPetsArrayParams & { page: number },
  ]) => listPetsArray(pageParams, fetchOptions);

  const query = useSWRInfinite<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKeyLoader,
    swrFn,
    swrOptions,
  );

  return {
    swrKeyLoader,
    ...query,
  };
};

export type ListPetsArrayQueryResult = NonNullable<
  Awaited<ReturnType<typeof listPetsArray>>
>;

/**
 * @summary List all pets as direct array
 */
export const useListPetsArray = <TError = Promise<unknown>>(
  params?: ListPetsArrayParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof listPetsArray>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    fetch?: RequestInit;
  },
) => {
  const { swr: swrOptions, fetch: fetchOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getListPetsArrayKey(params) : null));
  const swrFn = () => listPetsArray(params, fetchOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};

/**
 * @summary List all pets wrapped in data property
 */
export type listPetsWrappedResponse200 = {
  data: PetsWrappedResponse;
  status: 200;
};

export type listPetsWrappedResponseSuccess = listPetsWrappedResponse200 & {
  headers: Headers;
};
export type listPetsWrappedResponse = listPetsWrappedResponseSuccess;

export const getListPetsWrappedUrl = (params?: ListPetsWrappedParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/pets-wrapped?${stringifiedParams}`
    : `/pets-wrapped`;
};

export const listPetsWrapped = async (
  params?: ListPetsWrappedParams,
  options?: RequestInit,
): Promise<listPetsWrappedResponse> => {
  const res = await fetch(getListPetsWrappedUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: listPetsWrappedResponse['data'] = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  } as listPetsWrappedResponse;
};

export const getListPetsWrappedKey = (params?: ListPetsWrappedParams) =>
  [`/pets-wrapped`, ...(params ? [params] : [])] as const;
export const getListPetsWrappedInfiniteKeyLoader = (
  params?: ListPetsWrappedParams,
) => {
  return (
    page: number,
    previousPageData?: Awaited<ReturnType<typeof listPetsWrapped>>,
  ) => {
    if (previousPageData) {
      const responseData = previousPageData.data;
      if (!responseData) return null;

      // Direct array response (e.g., API returns Pet[]) - stop when empty
      if (Array.isArray(responseData) && responseData.length === 0) return null;

      // Wrapped response with data array (e.g., { data: Pet[], ... }) - stop when nested array is empty
      if (typeof responseData === 'object' && 'data' in responseData) {
        if (Array.isArray(responseData.data) && responseData.data.length === 0)
          return null;
      }

      // Single object response (non-paginated endpoint) - stop after first page
      if (
        !Array.isArray(responseData) &&
        !(typeof responseData === 'object' && 'data' in responseData)
      )
        return null;
    }

    return [
      `/pets-wrapped`,
      ...(params ? [{ ...params, page }] : [{ page }]),
    ] as const;
  };
};

export type ListPetsWrappedInfiniteQueryResult = NonNullable<
  Awaited<ReturnType<typeof listPetsWrapped>>
>;

/**
 * @summary List all pets wrapped in data property
 */
export const useListPetsWrappedInfinite = <TError = Promise<unknown>>(
  params?: ListPetsWrappedParams,
  options?: {
    swr?: SWRInfiniteConfiguration<
      Awaited<ReturnType<typeof listPetsWrapped>>,
      TError
    > & { swrKeyLoader?: SWRInfiniteKeyLoader; enabled?: boolean };
    fetch?: RequestInit;
  },
) => {
  const { swr: swrOptions, fetch: fetchOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKeyLoader =
    swrOptions?.swrKeyLoader ??
    (isEnabled ? getListPetsWrappedInfiniteKeyLoader(params) : () => null);
  const swrFn = ([_url, pageParams]: [
    string,
    ListPetsWrappedParams & { page: number },
  ]) => listPetsWrapped(pageParams, fetchOptions);

  const query = useSWRInfinite<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKeyLoader,
    swrFn,
    swrOptions,
  );

  return {
    swrKeyLoader,
    ...query,
  };
};

export type ListPetsWrappedQueryResult = NonNullable<
  Awaited<ReturnType<typeof listPetsWrapped>>
>;

/**
 * @summary List all pets wrapped in data property
 */
export const useListPetsWrapped = <TError = Promise<unknown>>(
  params?: ListPetsWrappedParams,
  options?: {
    swr?: SWRConfiguration<
      Awaited<ReturnType<typeof listPetsWrapped>>,
      TError
    > & { swrKey?: Key; enabled?: boolean };
    fetch?: RequestInit;
  },
) => {
  const { swr: swrOptions, fetch: fetchOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ??
    (() => (isEnabled ? getListPetsWrappedKey(params) : null));
  const swrFn = () => listPetsWrapped(params, fetchOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};

/**
 * @summary Get a single pet object
 */
export type getPetSingleResponse200 = {
  data: Pet;
  status: 200;
};

export type getPetSingleResponseSuccess = getPetSingleResponse200 & {
  headers: Headers;
};
export type getPetSingleResponse = getPetSingleResponseSuccess;

export const getGetPetSingleUrl = () => {
  return `/pet-single`;
};

export const getPetSingle = async (
  options?: RequestInit,
): Promise<getPetSingleResponse> => {
  const res = await fetch(getGetPetSingleUrl(), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: getPetSingleResponse['data'] = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  } as getPetSingleResponse;
};

export const getGetPetSingleKey = () => [`/pet-single`] as const;
export const getGetPetSingleInfiniteKeyLoader = () => {
  return (
    page: number,
    previousPageData?: Awaited<ReturnType<typeof getPetSingle>>,
  ) => {
    if (previousPageData) {
      const responseData = previousPageData.data;
      if (!responseData) return null;

      // Direct array response (e.g., API returns Pet[]) - stop when empty
      if (Array.isArray(responseData) && responseData.length === 0) return null;

      // Wrapped response with data array (e.g., { data: Pet[], ... }) - stop when nested array is empty
      if (typeof responseData === 'object' && 'data' in responseData) {
        if (Array.isArray(responseData.data) && responseData.data.length === 0)
          return null;
      }

      // Single object response (non-paginated endpoint) - stop after first page
      if (
        !Array.isArray(responseData) &&
        !(typeof responseData === 'object' && 'data' in responseData)
      )
        return null;
    }

    return [`/pet-single`] as const;
  };
};

export type GetPetSingleInfiniteQueryResult = NonNullable<
  Awaited<ReturnType<typeof getPetSingle>>
>;

/**
 * @summary Get a single pet object
 */
export const useGetPetSingleInfinite = <TError = Promise<unknown>>(options?: {
  swr?: SWRInfiniteConfiguration<
    Awaited<ReturnType<typeof getPetSingle>>,
    TError
  > & { swrKeyLoader?: SWRInfiniteKeyLoader; enabled?: boolean };
  fetch?: RequestInit;
}) => {
  const { swr: swrOptions, fetch: fetchOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKeyLoader =
    swrOptions?.swrKeyLoader ??
    (isEnabled ? getGetPetSingleInfiniteKeyLoader() : () => null);
  const swrFn = ([_url]: [string]) => getPetSingle(fetchOptions);

  const query = useSWRInfinite<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKeyLoader,
    swrFn,
    swrOptions,
  );

  return {
    swrKeyLoader,
    ...query,
  };
};

export type GetPetSingleQueryResult = NonNullable<
  Awaited<ReturnType<typeof getPetSingle>>
>;

/**
 * @summary Get a single pet object
 */
export const useGetPetSingle = <TError = Promise<unknown>>(options?: {
  swr?: SWRConfiguration<Awaited<ReturnType<typeof getPetSingle>>, TError> & {
    swrKey?: Key;
    enabled?: boolean;
  };
  fetch?: RequestInit;
}) => {
  const { swr: swrOptions, fetch: fetchOptions } = options ?? {};

  const isEnabled = swrOptions?.enabled !== false;
  const swrKey =
    swrOptions?.swrKey ?? (() => (isEnabled ? getGetPetSingleKey() : null));
  const swrFn = () => getPetSingle(fetchOptions);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions,
  );

  return {
    swrKey,
    ...query,
  };
};
