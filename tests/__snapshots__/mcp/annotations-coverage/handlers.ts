/**
 * Generated by orval 🍺
 * Do not edit manually.
 * annotations
 * OpenAPI spec version: 1
 */
export type CreateThingBody = { [key: string]: unknown };

export type ReplaceThingBody = { [key: string]: unknown };

export type PatchThingBody = { [key: string]: unknown };

import {
  getThings,
  createThing,
  replaceThing,
  patchThing,
  deleteThing,
  optionsThings,
  headThings,
} from './http-client';

export const getThingsHandler = async (
  options: RequestInit,
  toStructuredContent: (
    data: unknown,
  ) =>
    | { success: true; data: Record<string, unknown> | undefined }
    | { success: false; error: { message: string } },
) => {
  const res = await getThings(options);

  if (res.status >= 400) {
    return {
      content: [
        {
          type: 'text' as const,
          text: JSON.stringify(res.data ?? null),
        },
      ],
      isError: true,
    };
  }

  const text = JSON.stringify(res.data ?? null);
  const result = toStructuredContent(res.data);

  return result.success
    ? {
        content: [{ type: 'text' as const, text }],
        structuredContent: result.data,
      }
    : {
        content: [
          { type: 'text' as const, text },
          { type: 'text' as const, text: result.error.message },
        ],
        isError: true,
      };
};

export type createThingArgs = {
  bodyParams?: CreateThingBody;
};

export const createThingHandler = async (
  args: createThingArgs,
  options: RequestInit,
  toStructuredContent: (
    data: unknown,
  ) =>
    | { success: true; data: Record<string, unknown> | undefined }
    | { success: false; error: { message: string } },
) => {
  const res = await createThing(args.bodyParams, options);

  if (res.status >= 400) {
    return {
      content: [
        {
          type: 'text' as const,
          text: JSON.stringify(res.data ?? null),
        },
      ],
      isError: true,
    };
  }

  const text = JSON.stringify(res.data ?? null);
  const result = toStructuredContent(res.data);

  return result.success
    ? {
        content: [{ type: 'text' as const, text }],
        structuredContent: result.data,
      }
    : {
        content: [
          { type: 'text' as const, text },
          { type: 'text' as const, text: result.error.message },
        ],
        isError: true,
      };
};

export type replaceThingArgs = {
  bodyParams?: ReplaceThingBody;
};

export const replaceThingHandler = async (
  args: replaceThingArgs,
  options: RequestInit,
  toStructuredContent: (
    data: unknown,
  ) =>
    | { success: true; data: Record<string, unknown> | undefined }
    | { success: false; error: { message: string } },
) => {
  const res = await replaceThing(args.bodyParams, options);

  if (res.status >= 400) {
    return {
      content: [
        {
          type: 'text' as const,
          text: JSON.stringify(res.data ?? null),
        },
      ],
      isError: true,
    };
  }

  const text = JSON.stringify(res.data ?? null);
  const result = toStructuredContent(res.data);

  return result.success
    ? {
        content: [{ type: 'text' as const, text }],
        structuredContent: result.data,
      }
    : {
        content: [
          { type: 'text' as const, text },
          { type: 'text' as const, text: result.error.message },
        ],
        isError: true,
      };
};

export type patchThingArgs = {
  bodyParams?: PatchThingBody;
};

export const patchThingHandler = async (
  args: patchThingArgs,
  options: RequestInit,
  toStructuredContent: (
    data: unknown,
  ) =>
    | { success: true; data: Record<string, unknown> | undefined }
    | { success: false; error: { message: string } },
) => {
  const res = await patchThing(args.bodyParams, options);

  if (res.status >= 400) {
    return {
      content: [
        {
          type: 'text' as const,
          text: JSON.stringify(res.data ?? null),
        },
      ],
      isError: true,
    };
  }

  const text = JSON.stringify(res.data ?? null);
  const result = toStructuredContent(res.data);

  return result.success
    ? {
        content: [{ type: 'text' as const, text }],
        structuredContent: result.data,
      }
    : {
        content: [
          { type: 'text' as const, text },
          { type: 'text' as const, text: result.error.message },
        ],
        isError: true,
      };
};

export const deleteThingHandler = async (
  options: RequestInit,
  toStructuredContent: (
    data: unknown,
  ) =>
    | { success: true; data: Record<string, unknown> | undefined }
    | { success: false; error: { message: string } },
) => {
  const res = await deleteThing(options);

  if (res.status >= 400) {
    return {
      content: [
        {
          type: 'text' as const,
          text: JSON.stringify(res.data ?? null),
        },
      ],
      isError: true,
    };
  }

  const text = JSON.stringify(res.data ?? null);
  const result = toStructuredContent(res.data);

  return result.success
    ? {
        content: [{ type: 'text' as const, text }],
        structuredContent: result.data,
      }
    : {
        content: [
          { type: 'text' as const, text },
          { type: 'text' as const, text: result.error.message },
        ],
        isError: true,
      };
};

export const optionsThingsHandler = async (
  options: RequestInit,
  toStructuredContent: (
    data: unknown,
  ) =>
    | { success: true; data: Record<string, unknown> | undefined }
    | { success: false; error: { message: string } },
) => {
  const res = await optionsThings(options);

  if (res.status >= 400) {
    return {
      content: [
        {
          type: 'text' as const,
          text: JSON.stringify(res.data ?? null),
        },
      ],
      isError: true,
    };
  }

  const text = JSON.stringify(res.data ?? null);
  const result = toStructuredContent(res.data);

  return result.success
    ? {
        content: [{ type: 'text' as const, text }],
        structuredContent: result.data,
      }
    : {
        content: [
          { type: 'text' as const, text },
          { type: 'text' as const, text: result.error.message },
        ],
        isError: true,
      };
};

export const headThingsHandler = async (
  options: RequestInit,
  toStructuredContent: (
    data: unknown,
  ) =>
    | { success: true; data: Record<string, unknown> | undefined }
    | { success: false; error: { message: string } },
) => {
  const res = await headThings(options);

  if (res.status >= 400) {
    return {
      content: [
        {
          type: 'text' as const,
          text: JSON.stringify(res.data ?? null),
        },
      ],
      isError: true,
    };
  }

  const text = JSON.stringify(res.data ?? null);
  const result = toStructuredContent(res.data);

  return result.success
    ? {
        content: [{ type: 'text' as const, text }],
        structuredContent: result.data,
      }
    : {
        content: [
          { type: 'text' as const, text },
          { type: 'text' as const, text: result.error.message },
        ],
        isError: true,
      };
};
