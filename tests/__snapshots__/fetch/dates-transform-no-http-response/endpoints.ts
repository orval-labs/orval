/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Dates transform
 * OpenAPI spec version: 1.0.0
 */
import type {
  Appointment,
  AppointmentWithDuplicateDate,
  AuditRecord,
  Cat,
  Dog,
  LastInspection,
  OrderDetails,
  ReminderUpdate,
  ShelterIntake,
} from './model';

export const getGetOrderDetailsUrl = (orderId: string) => {
  return `/orders/${orderId}`;
};

export const getOrderDetails = async (
  orderId: string,
  options?: RequestInit,
): Promise<OrderDetails> => {
  const res = await fetch(getGetOrderDetailsUrl(orderId), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: any; status?: number } =
      new globalThis.Error();
    const data = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  let data: OrderDetails = body ? JSON.parse(body) : {};
  if (body && res.status === 200) {
    data = deserializeGetOrderDetailsResponse(data as OrderDetails);
  }
  return data;
};

const deserializeGetOrderDetailsResponse = (
  data: OrderDetails,
): OrderDetails => {
  if (data == null) return data;
  data.placedAt = new Date(data.placedAt);
  if (data.deliveredAt != null) {
    data.deliveredAt = new Date(data.deliveredAt);
  }
  if (data.lastViewedAt != null) {
    data.lastViewedAt = new Date(data.lastViewedAt);
  }
  if (data.events != null) {
    for (let i0 = 0; i0 < data.events.length; i0++) {
      const item0 = data.events[i0];
      item0.createdAt = new Date(item0.createdAt);
      if (item0.resolvedAt != null) {
        item0.resolvedAt = new Date(item0.resolvedAt);
      }
    }
  }
  return data;
};

export const getGetPetProfileUrl = (petId: string) => {
  return `/pets/${petId}`;
};

export const getPetProfile = async (
  petId: string,
  options?: RequestInit,
): Promise<Cat | Dog> => {
  const res = await fetch(getGetPetProfileUrl(petId), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: any; status?: number } =
      new globalThis.Error();
    const data = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  let data: Cat | Dog = body ? JSON.parse(body) : {};
  if (body && res.status === 200) {
    data = deserializeGetPetProfileResponse(data as Cat | Dog);
  }
  return data;
};

const deserializeGetPetProfileResponse = (data: Cat | Dog): Cat | Dog => {
  if (data == null) return data;
  switch (data.petType) {
    case 'cat': {
      data.vaccinatedAt = new Date(data.vaccinatedAt);
      break;
    }
    case 'dog': {
      if (data.adoptedAt != null) {
        data.adoptedAt = new Date(data.adoptedAt);
      }
      break;
    }
  }
  return data;
};

export const getGetAuditRecordUrl = (auditId: string) => {
  return `/audits/${auditId}`;
};

export const getAuditRecord = async (
  auditId: string,
  options?: RequestInit,
): Promise<AuditRecord> => {
  const res = await fetch(getGetAuditRecordUrl(auditId), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: any; status?: number } =
      new globalThis.Error();
    const data = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  let data: AuditRecord = body ? JSON.parse(body) : {};
  if (body && res.status === 200) {
    data = deserializeGetAuditRecordResponse(data as AuditRecord);
  }
  return data;
};

const deserializeGetAuditRecordResponse = (data: AuditRecord): AuditRecord => {
  if (data == null) return data;
  (
    data as { -readonly [K in keyof typeof data]: (typeof data)[K] }
  ).recordedAt = new Date(data.recordedAt);
  if (data.stamps != null) {
    for (let i0 = 0; i0 < data.stamps.length; i0++) {
      data.stamps[i0] = new Date(data.stamps[i0]);
    }
  }
  if (data.entries != null) {
    for (let i0 = 0; i0 < data.entries.length; i0++) {
      const item0 = data.entries[i0];
      (
        item0 as { -readonly [K in keyof typeof item0]: (typeof item0)[K] }
      ).changedAt = new Date(item0.changedAt);
    }
  }
  return data;
};

export const getListShelterPetsUrl = (shelterId: string) => {
  return `/shelters/${shelterId}/pets`;
};

export const listShelterPets = async (
  shelterId: string,
  options?: RequestInit,
): Promise<Cat | Dog> => {
  const res = await fetch(getListShelterPetsUrl(shelterId), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: any; status?: number } =
      new globalThis.Error();
    const data = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  let data: Cat | Dog = body ? JSON.parse(body) : {};
  if (body && res.status === 200) {
    data = deserializeListShelterPetsResponse(data as Cat | Dog);
  }
  return data;
};

const deserializeListShelterPetsResponse = (data: Cat | Dog): Cat | Dog => {
  if (data == null) return data;
  switch (data.petType) {
    case 'cat': {
      data.vaccinatedAt = new Date(data.vaccinatedAt);
      break;
    }
    case 'dog': {
      if (data.adoptedAt != null) {
        data.adoptedAt = new Date(data.adoptedAt);
      }
      break;
    }
  }
  return data;
};

export const getGetShelterLastInspectionUrl = (shelterId: string) => {
  return `/shelters/${shelterId}/last-inspection`;
};

export const getShelterLastInspection = async (
  shelterId: string,
  options?: RequestInit,
): Promise<LastInspection> => {
  const res = await fetch(getGetShelterLastInspectionUrl(shelterId), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: any; status?: number } =
      new globalThis.Error();
    const data = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  let data: LastInspection = body ? JSON.parse(body) : {};
  if (body && res.status === 200) {
    data = deserializeGetShelterLastInspectionResponse(data as LastInspection);
  }
  return data;
};

const deserializeGetShelterLastInspectionResponse = (
  data: LastInspection,
): LastInspection => {
  if (data == null) return data;
  data = new Date(data);
  return data;
};

export const getListOrderNumbersUrl = () => {
  return `/orders`;
};

export const listOrderNumbers = async (
  options?: RequestInit,
): Promise<string[]> => {
  const res = await fetch(getListOrderNumbersUrl(), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: any; status?: number } =
      new globalThis.Error();
    const data = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  const data: string[] = body ? JSON.parse(body) : {};
  return data;
};

export const getUpdateAppointmentUrl = () => {
  return `/appointments`;
};

export const updateAppointment = async (
  appointment: Appointment,
  options?: RequestInit,
): Promise<Appointment> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
  const res = await fetch(getUpdateAppointmentUrl(), {
    ...options,
    method: 'PUT',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(serializeUpdateAppointmentRequest(appointment)),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: any; status?: number } =
      new globalThis.Error();
    const data = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  let data: Appointment = body ? JSON.parse(body) : {};
  if (body && res.status === 200) {
    data = deserializeUpdateAppointmentResponse(data as Appointment);
  }
  return data;
};

const deserializeUpdateAppointmentResponse = (
  data: Appointment,
): Appointment => {
  if (data == null) return data;
  data.day = new Date(data.day);
  data.bookedAt = new Date(data.bookedAt);
  if (data.reminderOn != null) {
    data.reminderOn = new Date(data.reminderOn);
  }
  if (data.slots != null) {
    for (let i0 = 0; i0 < data.slots.length; i0++) {
      const item0 = data.slots[i0];
      item0.start = new Date(item0.start);
    }
  }
  return data;
};

const serializeUpdateAppointmentRequest = (data: Appointment): Appointment => {
  if (data == null) return data;
  const copy = { ...data };
  copy.day =
    copy.day instanceof Date
      ? (copy.day.toISOString().slice(0, 10) as unknown as Date)
      : copy.day;
  if (copy.reminderOn != null) {
    copy.reminderOn =
      copy.reminderOn instanceof Date
        ? (copy.reminderOn.toISOString().slice(0, 10) as unknown as Date)
        : copy.reminderOn;
  }
  if (copy.slots != null) {
    copy.slots = copy.slots.map((item0) => {
      let value0 = item0;
      value0 = { ...value0 };
      value0.start =
        value0.start instanceof Date
          ? (value0.start.toISOString().slice(0, 10) as unknown as Date)
          : value0.start;
      return value0;
    });
  }
  return copy;
};

export const getUpdateAppointmentDuplicateDateUrl = () => {
  return `/appointments/duplicate-date`;
};

export const updateAppointmentDuplicateDate = async (
  appointmentWithDuplicateDate: AppointmentWithDuplicateDate,
  options?: RequestInit,
): Promise<AppointmentWithDuplicateDate> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
  const res = await fetch(getUpdateAppointmentDuplicateDateUrl(), {
    ...options,
    method: 'PUT',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(
      serializeUpdateAppointmentDuplicateDateRequest(
        appointmentWithDuplicateDate,
      ),
    ),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: any; status?: number } =
      new globalThis.Error();
    const data = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  let data: AppointmentWithDuplicateDate = body ? JSON.parse(body) : {};
  if (body && res.status === 200) {
    data = deserializeUpdateAppointmentDuplicateDateResponse(
      data as AppointmentWithDuplicateDate,
    );
  }
  return data;
};

const deserializeUpdateAppointmentDuplicateDateResponse = (
  data: AppointmentWithDuplicateDate,
): AppointmentWithDuplicateDate => {
  if (data == null) return data;
  data.day = new Date(data.day);
  data.day = new Date(data.day);
  return data;
};

const serializeUpdateAppointmentDuplicateDateRequest = (
  data: AppointmentWithDuplicateDate,
): AppointmentWithDuplicateDate => {
  if (data == null) return data;
  const copy = { ...data };
  copy.day =
    copy.day instanceof Date
      ? (copy.day.toISOString().slice(0, 10) as unknown as Date)
      : copy.day;
  copy.day =
    copy.day instanceof Date
      ? (copy.day.toISOString().slice(0, 10) as unknown as Date)
      : copy.day;
  return copy;
};

export const getUpdateAppointmentReminderUrl = (appointmentId: string) => {
  return `/appointments/${appointmentId}/reminder`;
};

export const updateAppointmentReminder = async (
  appointmentId: string,
  reminderUpdate?: ReminderUpdate,
  options?: RequestInit,
): Promise<Appointment> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
  const res = await fetch(getUpdateAppointmentReminderUrl(appointmentId), {
    ...options,
    method: 'PATCH',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(
      serializeUpdateAppointmentReminderRequest(reminderUpdate),
    ),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: any; status?: number } =
      new globalThis.Error();
    const data = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  let data: Appointment = body ? JSON.parse(body) : {};
  if (body && res.status === 200) {
    data = deserializeUpdateAppointmentReminderResponse(data as Appointment);
  }
  return data;
};

const deserializeUpdateAppointmentReminderResponse = (
  data: Appointment,
): Appointment => {
  if (data == null) return data;
  data.day = new Date(data.day);
  data.bookedAt = new Date(data.bookedAt);
  if (data.reminderOn != null) {
    data.reminderOn = new Date(data.reminderOn);
  }
  if (data.slots != null) {
    for (let i0 = 0; i0 < data.slots.length; i0++) {
      const item0 = data.slots[i0];
      item0.start = new Date(item0.start);
    }
  }
  return data;
};

const serializeUpdateAppointmentReminderRequest = (
  data: ReminderUpdate | undefined,
): ReminderUpdate | undefined => {
  if (data == null) return data;
  const copy = { ...data };
  if (copy.remindOn != null) {
    copy.remindOn =
      copy.remindOn instanceof Date
        ? (copy.remindOn.toISOString().slice(0, 10) as unknown as Date)
        : copy.remindOn;
  }
  return copy;
};

export const getUpdateShelterIntakeUrl = (shelterId: string) => {
  return `/shelters/${shelterId}/intake`;
};

export const updateShelterIntake = async (
  shelterId: string,
  shelterIntake: ShelterIntake,
  options?: RequestInit,
): Promise<ShelterIntake> => {
  const getHeaders = (
    h?: NonNullable<RequestInit['headers']>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<
      string | readonly string[] | undefined
    >(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
  const res = await fetch(getUpdateShelterIntakeUrl(shelterId), {
    ...options,
    method: 'PUT',
    headers: {
      'Content-Type': 'application/json',
      ...getHeaders(options?.headers),
    },
    body: JSON.stringify(serializeUpdateShelterIntakeRequest(shelterIntake)),
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: any; status?: number } =
      new globalThis.Error();
    const data = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  let data: ShelterIntake = body ? JSON.parse(body) : {};
  if (body && res.status === 200) {
    data = deserializeUpdateShelterIntakeResponse(data as ShelterIntake);
  }
  return data;
};

const deserializeUpdateShelterIntakeResponse = (
  data: ShelterIntake,
): ShelterIntake => {
  if (data == null) return data;
  if (data.pets != null) {
    for (const key0 of Object.keys(data.pets)) {
      const item0 = data.pets[key0];
      switch (item0.petType) {
        case 'cat': {
          item0.arrivedOn = new Date(item0.arrivedOn);
          break;
        }
        case 'dog': {
          item0.vaccinatedAt = new Date(item0.vaccinatedAt);
          break;
        }
      }
    }
  }
  return data;
};

const serializeUpdateShelterIntakeRequest = (
  data: ShelterIntake,
): ShelterIntake => {
  if (data == null) return data;
  const copy = { ...data };
  if (copy.pets != null) {
    copy.pets = { ...copy.pets };
    for (const key0 of Object.keys(copy.pets)) {
      let value0 = copy.pets[key0];
      value0 = { ...value0 };
      switch (value0.petType) {
        case 'cat': {
          value0.arrivedOn =
            value0.arrivedOn instanceof Date
              ? (value0.arrivedOn.toISOString().slice(0, 10) as unknown as Date)
              : value0.arrivedOn;
          break;
        }
      }
      copy.pets[key0] = value0;
    }
  }
  return copy;
};
