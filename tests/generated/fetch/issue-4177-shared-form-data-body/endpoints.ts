/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Issue #4177 — multipart body aliased as a shared schema
 * OpenAPI spec version: 1.0.0
 */
import type { Metadata, Upload, UploadInlineBody } from './model';

export type uploadSharedResponse204 = {
  data: void;
  status: 204;
};

export type uploadSharedResponseSuccess = uploadSharedResponse204 & {
  headers: Headers;
};
export type uploadSharedResponse = uploadSharedResponseSuccess;

export const getUploadSharedUrl = () => {
  return `/upload`;
};

export const uploadShared = async (
  upload: Upload,
  options?: RequestInit,
): Promise<uploadSharedResponse> => {
  const formData = new FormData();
  if (upload.bar !== undefined) {
    formData.append(`bar`, upload.bar);
  }
  if (upload.report !== undefined) {
    formData.append(
      `report`,
      upload.report instanceof Blob
        ? upload.report
        : new Blob([upload.report], { type: 'text/csv' }),
    );
  }
  if (upload.avatar !== undefined) {
    formData.append(`avatar`, upload.avatar);
  }
  if (upload.files !== undefined) {
    upload.files.forEach((value) => formData.append(`files`, value));
  }

  const res = await fetch(getUploadSharedUrl(), {
    ...options,
    method: 'POST',
    body: formData,
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: uploadSharedResponse['data'] = body
    ? JSON.parse(body)
    : undefined;
  return {
    data,
    status: res.status,
    headers: res.headers,
  } as uploadSharedResponse;
};

export type uploadInlineResponse204 = {
  data: void;
  status: 204;
};

export type uploadInlineResponseSuccess = uploadInlineResponse204 & {
  headers: Headers;
};
export type uploadInlineResponse = uploadInlineResponseSuccess;

export const getUploadInlineUrl = () => {
  return `/upload-inline`;
};

export const uploadInline = async (
  uploadInlineBody: UploadInlineBody,
  options?: RequestInit,
): Promise<uploadInlineResponse> => {
  const formData = new FormData();
  if (uploadInlineBody.bar !== undefined) {
    formData.append(`bar`, uploadInlineBody.bar);
  }
  if (uploadInlineBody.files !== undefined) {
    uploadInlineBody.files.forEach((value) => formData.append(`files`, value));
  }

  const res = await fetch(getUploadInlineUrl(), {
    ...options,
    method: 'POST',
    body: formData,
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: uploadInlineResponse['data'] = body
    ? JSON.parse(body)
    : undefined;
  return {
    data,
    status: res.status,
    headers: res.headers,
  } as uploadInlineResponse;
};

export type getMetadataResponse200 = {
  data: Metadata;
  status: 200;
};

export type getMetadataResponseSuccess = getMetadataResponse200 & {
  headers: Headers;
};
export type getMetadataResponse = getMetadataResponseSuccess;

export const getGetMetadataUrl = () => {
  return `/metadata`;
};

export const getMetadata = async (
  options?: RequestInit,
): Promise<getMetadataResponse> => {
  const res = await fetch(getGetMetadataUrl(), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();

  const data: getMetadataResponse['data'] = body ? JSON.parse(body) : {};
  return {
    data,
    status: res.status,
    headers: res.headers,
  } as getMetadataResponse;
};
