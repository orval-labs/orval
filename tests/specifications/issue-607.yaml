openapi: 3.0.0
info:
  title: issue-607
  version: 1.0.0
paths:
  /pet/{petId}/{petIdRequestFn}/same-name:
    post:
      operationId: petId
      parameters:
        - name: petId
          in: path
          required: true
          schema:
            type: integer
        - name: petIdRequestFn
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
  /pet/{petId}/options:
    post:
      operationId: options
      parameters:
        - name: petId
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
components:
  schemas:
    ApiResponse:
      type: object
      properties:
        code:
          type: integer
        message:
          type: string
