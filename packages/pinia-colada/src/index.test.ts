import { expect, it } from 'vite-plus/test';

// Placeholder so Vitest does not print "No test files found".
// Delete this when real tests are added.
it('has a test file', () => {
  expect(true).toBe(true);
});
