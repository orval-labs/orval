---
title: Vue Query
description: 从 OpenAPI 生成类型安全的 Vue Query Composables（组合式函数）
---

根据你的 OpenAPI 规范生成完整类型化的 [TanStack Query for Vue](https://tanstack.com/query/latest/docs/vue/overview) Composables（组合式函数）。

## 配置

将 `client` 选项设置为 `vue-query`：

```ts title="orval.config.ts"
import { defineConfig } from 'orval';

export default defineConfig({
  petstore: {
    output: {
      mode: 'tags-split',
      target: 'src/api/petstore.ts',
      schemas: 'src/api/model',
      client: 'vue-query',
      mock: true,
    },
    input: {
      target: './petstore.yaml',
    },
  },
});
```

## 生成结果

Orval 会为你的 OpenAPI 规范中的每个 path 生成一个 Composable（组合式函数）。例如，[这个规范](https://github.com/orval-labs/orval/blob/master/samples/vue-query/petstore.yaml)会生成：

```ts
export const showPetById = (
  petId: string,
  options?: AxiosRequestConfig,
): Promise<AxiosResponse<Pet>> => {
  return axios.get(`/pets/${petId}`, options);
};

export const getShowPetByIdQueryKey = (petId: string) => [`/pets/${petId}`];

export const useShowPetById = <
  TData = AsyncReturnType<typeof showPetById>,
  TError = Error,
>(
  petId: string,
  options?: {
    query?: UseQueryOptions<AsyncReturnType<typeof showPetById>, TError, TData>;
    axios?: AxiosRequestConfig;
  },
) => {
  const { query: queryOptions, axios: axiosOptions } = options ?? {};

  const queryKey = queryOptions?.queryKey ?? getShowPetByIdQueryKey(petId);
  const queryFn = () => showPetById(petId, axiosOptions);

  const query = useQuery<AsyncReturnType<typeof queryFn>, TError, TData>(
    queryKey,
    queryFn,
    {
      enabled: petId !== null && petId !== undefined,
      ...queryOptions,
    },
  );

  return {
    queryKey,
    ...query,
  };
};
```

## 响应式参数

生成的 Composables（组合式函数）接受响应式参数，因此当输入变化时 Query 会重新执行。在 **Vue Query v5** 下（它要求 Vue 3.3+），每个参数都会被类型化为 [`MaybeRefOrGetter<T>`](https://vuejs.org/api/utility-types.html#maybereforgetter)，也就是 `T | Ref<T> | (() => T)`，并在内部通过 [`toValue()`](https://vuejs.org/api/reactivity-utilities.html#tovalue) 解包。因此你可以直接传 Getter（取值函数），而不需要额外包一层 `computed`：

```ts
// 这三种形式都可以：
useShowPetById(petId.value);
useShowPetById(petId);
useShowPetById(() => props.petId);
```

`MaybeRefOrGetter<T>` 是 `MaybeRef<T>` 的超集，因此现有那些传入 ref 或普通值的调用点都可以继续不做修改地工作。针对 Vue Query v4（或旧版 `vue-query` v3 package）生成的项目，则仍会使用 `MaybeRef<T>`/`unref()`，因为这些目标可能运行在早于 3.3 的 Vue 版本上。

## Infinite Queries（无限查询）

生成 `useInfiniteQuery` Composables（组合式函数）：

```ts title="orval.config.ts"
import { defineConfig } from 'orval';

export default defineConfig({
  petstore: {
    output: {
      client: 'vue-query',
      override: {
        query: {
          useQuery: true,
          useInfinite: true,
          useInfiniteQueryParam: 'nextId',
          options: {
            staleTime: 10000,
          },
        },
      },
    },
    input: {
      target: './petstore.yaml',
    },
  },
});
```

## 单个操作覆盖

为特定操作覆盖 Query 选项：

```ts title="orval.config.ts"
import { defineConfig } from 'orval';

export default defineConfig({
  petstore: {
    output: {
      client: 'vue-query',
      override: {
        operations: {
          listPets: {
            query: {
              useInfinite: true,
            },
          },
        },
      },
    },
    input: {
      target: './petstore.yaml',
    },
  },
});
```

## Set Query Data（设置查询数据）

当设置 `useSetQueryData: true` 时，Orval 会生成类型安全的辅助函数，用来更新缓存中的 query data（查询数据）：

```ts
export const setListPetsQueryData = (
  queryClient: QueryClient,
  params: MaybeRefOrGetter<ListPetsParams> | undefined,
  updater:
    | Awaited<ReturnType<typeof listPets>>
    | undefined
    | ((
        old: Awaited<ReturnType<typeof listPets>> | undefined,
      ) => Awaited<ReturnType<typeof listPets>> | undefined),
  $exactMatch: boolean = true,
) => {
  queryClient.setQueriesData<Awaited<ReturnType<typeof listPets>>>(
    { exact: $exactMatch, queryKey: getListPetsQueryKey(params) },
    updater,
  );
};
```

这个辅助函数使用的是 [`setQueriesData`](https://tanstack.com/query/latest/docs/reference/QueryClient#queryclientsetqueriesdata)，因此 query key（查询键）可以按前缀匹配。最后一个参数 `$exactMatch` 默认为 `true`，即默认只更新 key 完全一致的那一条缓存；传入 `$exactMatch: false` 则会更新共享该路径前缀的所有缓存条目。对于带 query params（查询参数）或 body 的 endpoint，这些参数会被放宽为可接受 `undefined`：同时传入 `undefined` 与 `$exactMatch: false`，即可更新共享同一路径的所有缓存条目，而 `updater` 会对每个命中的条目各调用一次。

<Callout type="warn">
  在 8.11.0 之前，该辅助函数调用的是 `setQueryData`，它会写入（并创建）单个精确 key 对应的条目。自 8.11.0 起改为调用 `setQueriesData`，它只会更新已经存在的条目（不会创建）；本次变更后默认按 key 完全匹配，可传入 `$exactMatch: false` 改为按前缀匹配。升级时请检查现有调用点的行为是否符合预期。
</Callout>

## Get Query Data（获取查询数据）

当设置 `useGetQueryData: true` 时，Orval 会生成类型安全的辅助函数，用来读取缓存中的 query data（查询数据）：

```ts
export const getListPetsQueryData = (
  queryClient: QueryClient,
  params: ListPetsParams,
) =>
  queryClient.getQueryData<Awaited<ReturnType<typeof listPets>>>(
    getListPetsQueryKey(params),
  );
```

## 完整示例

请查看 GitHub 上的 [complete Vue Query example（完整 Vue Query 示例）](https://github.com/orval-labs/orval/tree/master/samples/vue-query)。
