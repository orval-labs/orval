// AUTO-GENERATED by `bun run gen:config-reference`. Do not edit by hand.
// Source: packages/core/src/types.ts
// Regenerate with: bun run gen:config-reference

import type { ConfigSection } from './types';

export const outputOverrideZod = {
  "section": "output.override.zod",
  "interfaceName": "ZodOptions",
  "fields": [
    {
      "name": "variant",
      "type": "ZodVariantOption",
      "optional": true,
      "description": "Select the generated Zod API style. `classic` imports from `zod`; `mini`\nimports from `zod/mini` and emits the functional/check-based Zod Mini API.\nZod Mini requires a Zod 4 target."
    },
    {
      "name": "version",
      "type": "ZodVersionOption",
      "optional": true,
      "description": "Pin the Zod output target so generation is deterministic instead of\ninferred from the installed `zod` version. Defaults to `'auto'`, which\ninfers from the detected package and otherwise falls back to Zod 4 output.\nSee {@link ZodVersionOption}."
    },
    {
      "name": "dateTimeOptions",
      "type": "ZodDateTimeOptions",
      "optional": true,
      "description": "Options for generated `datetime` format schemas."
    },
    {
      "name": "timeOptions",
      "type": "ZodTimeOptions",
      "optional": true,
      "description": "Options for generated `time` format schemas."
    },
    {
      "name": "generateEachHttpStatus",
      "type": "boolean",
      "optional": true,
      "description": "Emit a Zod schema for every HTTP status defined in the spec, not just the success one."
    },
    {
      "name": "generateReusableSchemas",
      "type": "boolean",
      "optional": true,
      "description": "When true, emits one reusable Zod schema per `#/components/schemas/*` `$ref`\n(with `namingConvention` applied to the name) and references it everywhere\ninstead of inlining. Default `false`. See `docs/superpowers/specs/2026-05-26-reusable-zod-schemas-design.md`."
    },
    {
      "name": "generateMeta",
      "type": "boolean",
      "optional": true,
      "description": "When true (zod v4 only), attaches registry metadata to generated\n**component** schemas via `.meta({ id, description?, deprecated? })`: `id` is\nthe schema name, plus `description`/`deprecated` when the OpenAPI schema\nprovides them. On zod v3 (which has no `.meta()`) descriptions still emit\nvia `.describe()`. Default `false`."
    },
    {
      "name": "generateDiscriminatedUnion",
      "type": "boolean",
      "optional": true,
      "description": "When true, a `oneOf`/`anyOf` that carries an OpenAPI `discriminator` is\nemitted as `zod.discriminatedUnion(key, [...])` (better per-branch errors)\ninstead of a plain `zod.union([...])`, but only when every branch can be\nrepresented as an object — otherwise it safely falls back to a union.\nDefault `false` (opt-in), so existing output is unchanged."
    },
    {
      "name": "exactOptional",
      "type": "boolean",
      "optional": true,
      "description": "When true (zod v4 only), emits optional object properties with\n`.exactOptional()` (classic) / `zod.exactOptional()` (mini) instead of\n`.optional()`, so consumers with `exactOptionalPropertyTypes` infer\n`{ x?: T }` rather than `{ x?: T | undefined }`. On zod v3 (which has no\n`.exactOptional()`) this is a no-op and `.optional()` is emitted. Default\n`false` (opt-in), so existing output is unchanged."
    }
  ]
} satisfies ConfigSection;
